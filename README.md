Reflection

Most Challenging Part

The most challenging part of this project was managing global state for favorites while keeping the application clean and scalable. Ensuring that favorites persisted correctly across sessions using localStorage while avoiding unnecessary re-renders required careful structuring of the Context and custom hooks.

Handling dynamic routing combined with API fetching (especially when navigating between routes quickly) was also a learning experience in managing loading and error states effectively.
